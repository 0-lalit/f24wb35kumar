extends layout

block content
  h1= title
  p Hello you #{title}
   button(type="button", class="btn btn-secondary") Press me
  button(onclick="boost()") Sample Button
  div#boost add in extra
  script.
    let value = 0;
    let amt = 3;
 
    function boost() {
        amt += 1;
        value += amt;
        document.getElementById("boost").innerText = `Boosted ${value} by ${amt}`;
    }